# Conductor

This directory is the repository-local execution ledger for autonomous and human
implementation. `CODEX_IMPLEMENTATION_PROMPT.md` supplies the substantive
workstreams; `tracks.yaml` supplies stable identifiers and phase gates.

When work changes state, keep `tracks.yaml`, `status.md`, and `runlog.md`
synchronized. A track is not complete until its acceptance evidence is recorded.
