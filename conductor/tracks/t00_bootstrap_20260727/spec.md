# Specification

Restore preserved Git history from the authoritative bundle, verify the source
ZIP against tag `v0.4.0`, resolve every pinned ecosystem repository without
mutating sibling worktrees, establish a green local and hosted baseline, safely
wire the private GitHub repository, and push the implementation branch.
