# Implementation plan

- [x] Add structured validation for empty identifiers, duplicate actions,
  invalid payoffs, and unreachable nodes (`9717125`).
- [x] Add canonical specification metadata and machine-readable validation paths
  (`eccfd73`).
- [~] Make tie policy and numeric tolerance explicit in solver results.
- [ ] Add deterministic chance-node and trace conformance tests.
- [ ] Implement normal-form pure best response and Nash enumeration.
- [ ] Inspect pinned UOGTO and Kairos APIs and prepare upstream-ready contracts.
- [ ] Run automated review and the complete repository quality gate.
- [ ] Record external ontology, Kairos, and extraction approval evidence; keep
  these pending until supplied.
